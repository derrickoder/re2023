import React from 'react'
import { Box } from '@mui/material';

const ProjectForm = () => {
    return (
        <React.Fragment>
            <Box>
                Add a Project
            </Box>
        </React.Fragment>
    )
};

export default ProjectForm;